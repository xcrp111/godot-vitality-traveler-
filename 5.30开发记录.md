# Godot Vitality Traveler 开发记录

> 日期：2026-05-31
> 修改范围：敌人生成系统重构 + 传送门 Bug 修复

---

## 一、修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `game-health 2026-5-30/jiaoben/enemy_spawner.gd` | **新建** | 可复用的房间敌人生成器 |
| `game-health 2026-5-30/jiaoben/changjingguanli.gd` | 修改 | 移除旧的随机坐标生成逻辑 |
| `game-health 2026-5-30/changjing/第一关.tscn` | 修改 | 添加 EnemySpawner 节点 + 3 个房间 |
| `game-health 2026-5-30/脚本/control.gd` | 修改 | 修复传送门按钮 Bug |

---

## 二、核心组件：EnemySpawner

### 位置
`res://jiaoben/enemy_spawner.gd`

### 功能
检测玩家所在房间，**只在玩家当前房间内生成敌人**。玩家通过传送门到另一个房间后，自动切换生成区域。

### 可配置参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `spawn_interval` | float | 4.0 | 每隔多少秒尝试生成一波 |
| `max_enemies_per_room` | int | 5 | 每个房间最多同时存在多少敌人 |
| `enemy_scenes` | Array[PackedScene] | 空 | 可生成的敌人类型列表（随机选取） |
| `cleanup_on_leave` | bool | false | 玩家离开房间后是否清除该房间的敌人 |

### 如何在新关卡复用

1. 在新关卡场景中 Add Child Node → `Node2D`，命名随意
2. 挂载脚本 `res://jiaoben/enemy_spawner.gd`
3. 在该节点下为**每个房间**添加一个子节点：
   ```
   EnemySpawner
   ├── Room1 (Area2D)
   │   └── CollisionShape2D (shape: RectangleShape2D，框出房间范围)
   ├── Room2 (Area2D)
   │   └── CollisionShape2D (shape: RectangleShape2D)
   └── ...（按需添加更多房间）
   ```
4. 在 Inspector 中配置：
   - `Enemy Scenes` → 拖入敌人 `.tscn` 文件
   - `Spawn Interval` → 设置生成频率
   - `Max Enemies Per Room` → 设置上限

### 如何引入新怪物

只需在 `enemy_scenes` 数组中**多拖一个** `.tscn` 进去即可。生成器会随机选取数组中的一种敌人生成。

---

## 三、场景管理器

### 位置
`res://jiaoben/changjingguanli.gd`

### 职责
- 菜单系统（打开/关闭/重新开始/退出）
- 计分（`add_score()` 方法供敌人死亡时调用）

### 敌人生成
已从场景管理器中移出，交由 `EnemySpawner` 组件独立处理。

---

## 四、传送门系统

### 传送流程
```
家园界面 → 走到传送门(door) → 点击交互按钮 → change_scene → 第一关
第一关内 → 走进 portal Area2D → 自动传送到 DestinationPoint
```

### 传送门配置位置
`res://changjing/portal.tscn` — 传送门场景模板

每个传送门的 Inspector 中可设置：
- `activation_delay`：激活延迟（秒），第一关前两个 10s，后两个 20s
- `DestinationPoint`：子节点 Marker2D，标记传送目标位置

---

## 五、已知待办

- [ ] 在编辑器中调整 Room1/Room2/Room3 的矩形框，对齐围墙实际位置（参照 TileMapLayer2）
- [ ] 如需第二关，复制 EnemySpawner 节点 → 调整房间矩形 → 拖入不同怪物
