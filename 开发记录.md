# Godot Vitality Traveler 开发记录

> 最后更新：2026-05-31

---

## 一、修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `jiaoben/enemy_spawner.gd` | **新建** | 房间敌人生成器（核心新系统） |
| `jiaoben/wall_builder.gd` | **新建** | @tool 墙壁生成器，勾复选框自动画墙 |
| `jiaoben/obstacle.gd` | **新建** | StaticBody2D 障碍物脚本（自动绘制色块） |
| `jiaoben/changjingguanli.gd` | 修改 | 移除旧的随机坐标生成，保留菜单/计分 |
| `changjing/第一关.tscn` | 修改 | 添加 EnemySpawner + 3个房间区域，删旧计时器 |
| `脚本/control.gd` | 修改 | 修复传送门按钮 Bug + 场景路径 |
| `Enemy/enemy.gd` | 修改 | 离屏检测改为实时取坐标，碰到 StaticBody2D 也转向 |
| `jiaoben/shilaimu.gd` | 修改 | 离屏检测改为实时取坐标，碰到 StaticBody2D 也转向 |

---

## 二、核心组件：EnemySpawner

### 位置
`res://jiaoben/enemy_spawner.gd`

### 原理
每个 tick 检测玩家在哪个房间的 Area2D 范围内，在该房间随机位置生成敌人。**碰撞体的位置和尺寸都会被使用**（`Area2D.global_position + CollisionShape2D.position` 为房间中心，`CollisionShape2D.shape.size` 为房间尺寸）。

### 可配置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `spawn_interval` | 2.0 | 生成间隔（秒） |
| `max_enemies_per_room` | 6 | 每个房间最多同时存在多少敌人 |
| `enemy_scenes` | 空 | 敌人类型列表，随机选取 |

### 如何在新关卡复用

1. 新场景中 Add Child → `Node2D`，挂 `enemy_spawner.gd`
2. 添加 Area2D 子节点（Room1, Room2...），每个放 CollisionShape2D（RectangleShape2D）
3. **移动 Area2D 节点**定位房间中心，**拖动矩形四角**调整房间范围
4. 在 Inspector 中配置 `enemy_scenes`、`spawn_interval`、`max_enemies_per_room`
5. 引入新怪物：在 `enemy_scenes` 数组中多拖一个 `.tscn` 即可

### 已知注意点
- 玩家必须处于 `group="player"` 且为 `CharacterBody2D` 类型
- 不要给其他节点添加 `player` 组（之前有个 TileMapLayer 误标导致 Bug）

---

## 三、墙壁生成器：WallBuilder

### 位置
`res://jiaoben/wall_builder.gd`

### 使用方法
1. 在场景根节点下 Add Child → `Node2D`，命名随意
2. 挂载 `wall_builder.gd`
3. Inspector 中勾选 **Build** → 墙壁自动画到 TileMapLayer2
4. Ctrl+S 保存场景 → 删除 WallBuilder 节点

### 当前障碍布局

| 房间 | 障碍 |
|------|------|
| Room1（起始区） | 中央 3×3 石柱 + L形墙角 |
| Room2（中区） | 三根石柱 + 横向路障 + 短墙 |
| Room3（末区） | C形围墙 + 两根石柱 + 窄走廊 |

所有墙壁使用 TileMapLayer2 的 atlas(1,1) 实心砖，自带物理碰撞，敌人碰到自动转向。

---

## 四、障碍物组件：Obstacle

### 位置
`res://jiaoben/obstacle.gd`

### 用途
挂到 StaticBody2D 上，自动根据子 CollisionShape2D 绘制深棕色矩形色块。适合手动添加不规则障碍物。

### 使用方法
1. Add Child → `StaticBody2D`，挂 `obstacle.gd`
2. 添加子 CollisionShape2D（RectangleShape2D），调整位置和大小
3. 可选：在 Inspector 中调整 `color` 和 `border_color`

---

## 五、传送门系统

### 传送流程
```
家园界面 → 走到传送门(door) → 点击交互按钮 → change_scene → 第一关
第一关内 → 走进 portal Area2D → 自动传送到 DestinationPoint
```

### 传送门配置
`res://changjing/portal.tscn` — 传送门场景模板

- `activation_delay`：激活延迟（秒），第一关前两个 10s，后两个 20s
- `DestinationPoint`：子节点 Marker2D，标记传送目标位置

### 第一关传送门映射

| 传送门 | 入口位置 | 目标位置 | 方向 |
|--------|---------|---------|------|
| portal | 房间1 | (742, 207) | 房间1→房间2 |
| portal2 | 房间1 | (302, 110) | 返回房间1 |
| portal3 | 房间2 | (2005, 180) | 房间2→房间3 |
| portal4 | 房间3 | (1608, 174) | 返回房间2 |

---

## 六、已修复的 Bug

### Bug 1：传送门按钮串线
- **现象**：家园界面点传送门弹出技能书对话框
- **原因**：`control.gd` 里 `interact_btn1.pressed` 连到了 `_on_interact`（技能书函数）
- **修复**：移除错误连接，让编辑器信号连接正常工作，同时修正目标场景路径

### Bug 2：Room2/3 不出怪（玩家坐标始终为 0,0）
- **现象**：EnemySpawner 打印玩家位置永远是 `(0, 0)`
- **原因**：TileMapLayer 被误标了 `groups=["player"]`，`get_first_node_in_group("player")` 拿到的是地图层
- **修复**：移除 TileMapLayer 的 player 组标记，EnemySpawner 改为只查找 CharacterBody2D

### Bug 3：Room2/3 敌人闪烁即消失
- **现象**：敌人在 Room2/3 生成后立即消失
- **原因**：`enemy.gd` / `shilaimu.gd` 在 `var mouse_pos = get_global_mouse_position()` 时节点未入场景树，返回 (0,0)，Room2/3 敌人 x>1000 被误判为离屏删除
- **修复**：改为每帧在 `check_wall_collision()` 中实时调用 `get_global_mouse_position()`

### Bug 4：场景文件损坏
- **现象**：添加障碍物节点后场景打不开
- **原因**：手动创建的 `.uid` 文件格式不合法
- **修复**：回退改动，改用 WallBuilder @tool 脚本方案
